

class InstallPlugin implements Plugin<Project> {
    void apply(Project project) {
        project.task('install') {
            dependsOn ':tasks:install'
            doLast {
                println 'install completed'
            }

        }
    }
}

apply plugin: InstallPlugin
